--- src/cirrus/hibernate/tools/SchemaExport.java.orig	Sat Mar 22 11:54:46 2003
+++ src/cirrus/hibernate/tools/SchemaExport.java	Wed May  7 14:16:56 2003
@@ -70,6 +70,17 @@
 	}
 	
 	/**
+	 * Run the schema creation script.
+	 * @param script write the script to standard out
+	 * @param export export the script to the database
+	 * @param delimiter end-of-line delimiter
+	 * @throws HibernateException
+	 */	
+	public void create(boolean script, boolean export, String delimiter) throws HibernateException {
+		execute(script, export, false, true, delimiter);
+	}
+
+	/**
 	 * Run the drop schema script.
 	 * @param script write the script to standard out
 	 * @param export export the script to the database
@@ -79,6 +90,17 @@
 		execute(script, export, true, true, null);
 	}
 	
+	/**
+	 * Run the drop schema script.
+	 * @param script write the script to standard out
+	 * @param export export the script to the database
+	 * @param delimiter end-of-line delimiter
+	 * @throws HibernateException
+	 */	
+	public void drop(boolean script, boolean export, String delimiter) throws HibernateException {
+		execute(script, export, true, true, delimiter);
+	}
+
 	private void execute(boolean script, boolean export, boolean justDrop, boolean format, String delimiter) throws HibernateException {
 		Connection connection = null;
 		FileWriter fileOutput = null;
--- src/cirrus/hibernate/tools/SchemaExportTask.java.orig	Sat Mar 22 11:54:48 2003
+++ src/cirrus/hibernate/tools/SchemaExportTask.java	Wed May  7 14:17:16 2003
@@ -152,10 +152,10 @@
 			SchemaExport schemaExport = getSchemaExport(ds);
 
 			if (drop) {
-				schemaExport.drop(!quiet, !text);
+				schemaExport.drop(!quiet, !text, delimiter);
 			} 
 			else {
-				schemaExport.create(!quiet, !text);
+				schemaExport.create(!quiet, !text, delimiter);
 			}
 		} 
 		catch (HibernateException e) {
