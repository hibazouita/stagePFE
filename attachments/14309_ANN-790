Index: org/hibernate/test/annotations/immutable/ImmutableTest.java
===================================================================
--- org/hibernate/test/annotations/immutable/ImmutableTest.java	(revisão 15675)
+++ org/hibernate/test/annotations/immutable/ImmutableTest.java	(cópia de trabalho)
@@ -60,6 +60,7 @@
 		germany = (Country) s.get(Country.class, country.getId());
 		assertNotNull(germany);
 		assertEquals("Name should not have changed", "Germany", germany.getName());
+		tx.commit();
 		s.close();
 		
 //		// try deletion
